# cl-tag

`cl-tag` 用于展示简短的标签内容，内容通过默认插槽传入。

## 基础使用

```html
<cl-tag>推荐</cl-tag>
```

## Attributes

当前没有组件专属 Attribute。

## Properties

当前没有组件专属 Property。

## Events

当前不派发自定义事件。

## Slots

| 名称 | 说明 |
| --- | --- |
| 默认插槽 | 标签显示内容 |

## 当前限制

- 当前没有颜色、尺寸或状态变体。
- 当前定位为展示组件，不包含选择、关闭等交互。
